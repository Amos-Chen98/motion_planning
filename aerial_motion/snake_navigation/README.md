# snake_navigation

A snake-inspired navigation package for multilink aerial robots. It uses a monolithic planner (`mono_planner`) to plan the root link’s motion and a multilink copilot (`multilink_copilot`) to coordinate the remaining links, enabling whole-body navigation in 3D environments.

## Dependencies

- `mono_planner`
- `multilink_copilot`
- `pcd_sim`